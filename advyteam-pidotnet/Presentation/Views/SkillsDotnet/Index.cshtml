@model IEnumerable<Data.skills>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descreption)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.note)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.skillsname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descreption)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.note)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.skillsname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idskill }) |
            @Html.ActionLink("Details", "Details", new { id=item.idskill }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idskill })
        </td>
    </tr>
}

</table>
